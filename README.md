# 🛸 UFO Tracker - 3AM INFRAMUNDO

Aplicación web para rastrear avistamientos OVNI en tiempo real.

## 🚀 Deploy

### Backend (Render)
- Root Directory: `backend`
- Build: `pip install -r requirements.txt`
- Start: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### Frontend (Netlify)
- Public folder: `frontend`
- Build command: (vacío)

*"La versión oficial nunca es la única"*